import Meanders.Problems.Open
import Meanders.Problems.Closed
import Meanders.Problems.Problem

/-! Umbrella for the Problems layer: what each kind of meander *is*, and the
`Problem` value that names them. Imports only. -/
